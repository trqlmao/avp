{
  "name": "avp-reference-client",
  "version": "0.0.0",
  "private": true,
  "description": "Reference client for the Alt Vault Protocol (HTTP/JSON profile). Drives the full lifecycle against a running server. Illustrative, not production.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "tsx src/client.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
